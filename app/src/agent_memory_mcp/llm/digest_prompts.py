"""Prompts for daily digest pipeline."""

CLUSTER_LABEL_PROMPT = """Определи общую тему для группы постов из Telegram-каналов.

Ответь строго в JSON формате:
{{"emoji": "<один подходящий emoji>", "label": "<название темы, 2-4 слова>"}}

Примеры:
{{"emoji": "🔧", "label": "Инструменты и сервисы"}}
{{"emoji": "🤖", "label": "AI и нейросети"}}
{{"emoji": "💰", "label": "Финансы и инвестиции"}}

Посты:
{posts}"""

# Default extraction policy (news-oriented). Used when no custom focus is given.
DEFAULT_DIGEST_FOCUS_BLOCK = """Из пакета постов Telegram-каналов извлеки ТОЛЬКО значимые новости, анонсы, технические инсайты.

СТРОГО игнорируй:
- Короткие реплики, вопросы, жалобы и обсуждения из чатов
- Рекламу, приветствия, дайджесты других каналов
- Посты без конкретной информации (мнения, шутки, эмоции)
- Дубликаты уже упомянутых тем

Оставляй только посты с реальной информационной ценностью: новые продукты, инструменты, технологии, исследования, важные цифры."""


def build_focus_block(focus: str | None) -> str:
    """Extraction policy for the MAP phase.

    With a custom focus, extraction is driven by the user's intent (which may
    legitimately include questions, decisions, deadlines, agreements — things
    the default news policy discards). Without it, use the default policy.
    """
    if focus and focus.strip():
        return (
            f"ФОКУС ДАЙДЖЕСТА: {focus.strip()}\n\n"
            "Извлекай в первую очередь то, что соответствует этому фокусу — "
            "включая вопросы, решения, договорённости, сроки, задачи, риски и "
            "любые важные для фокуса детали. Игнорируй только явный флуд и "
            "смолток, не относящийся к фокусу."
        )
    return DEFAULT_DIGEST_FOCUS_BLOCK


MAP_DIGEST_SYSTEM = """Ты — аналитик. Тема этого блока: {cluster_label}

{focus_block}

Для каждого значимого пункта запиши:
- Краткое описание (1-2 предложения)
- Ключевые факты, цифры, сроки, имена
- ID поста (telegram_msg_id) для ссылки

Ответь в формате:

ТЕМА: <название>
- <пункт 1> [msg_id: <id>]
- <пункт 2> [msg_id: <id>]

Посты:
{posts}"""

REDUCE_DIGEST_SYSTEM = """Ты — редактор дайджеста Telegram-каналов. Составь краткий, чистый дайджест.

Блоки уже сгруппированы по темам. Сохрани структуру кластеров и их заголовки.

Правила оформления:
- Каждый пункт = 1 строка, максимально кратко
- В конце каждого пункта ставь [msg_id: <id>] для ссылки на пост
- НЕ используй markdown-заголовки (###), только текст с emoji
- Сохрани тематические блоки как они даны (каждый блок уже имеет emoji + label)
- Каждый блок начинай с emoji и названия темы жирным: **emoji Название**
- Пункты начинай с точки: • текст [msg_id: N]
- Между блоками — пустая строка
- В конце укажи: *Дайджест на основе анализа {total_posts} постов*

Пример блока:
**🔧 Инструменты**
• Pencil.dev — инструмент для прототипирования интерфейсов [msg_id: 123]
• Wispr Flow — альтернатива для распознавания речи [msg_id: 456]

Извлечённые данные:"""
